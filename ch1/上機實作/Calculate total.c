/* Calculate total */
#include <stdio.h>
#include <stdlib.h>
int main (){
	int x=100, y=200, z=300;
	int total=0;
	total = x+y+z;
	printf("x=%d, y=%d, z=%d\n", x, y, z);
	printf("total = %d", total);
	system("PAUSE");
	return 0;
}
